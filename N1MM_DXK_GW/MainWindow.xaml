<ui:FluentWindow x:Class="N1MM_DXK_GW.MainWindow"
                 xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                 xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                 xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
                 xmlns:local="clr-namespace:N1MM_DXK_GW"
                 Title="{x:Static local:Strings.AppTitle}"
                 Width="720" Height="620"
                 MinWidth="560" MinHeight="480"
                 ExtendsContentIntoTitleBar="True"
                 WindowBackdropType="Mica"
                 WindowCornerPreference="Round"
                 WindowStartupLocation="CenterScreen">

   <Grid>
      <Grid.RowDefinitions>
         <RowDefinition Height="Auto" />
         <RowDefinition Height="*" />
      </Grid.RowDefinitions>

      <ui:TitleBar Grid.Row="0"
                   Title="{x:Static local:Strings.AppTitle}"
                   ShowMaximize="True" />

      <!-- Scaffold only. The settings, status, log and failed-QSO sections are
           ported in the next step; this proves the shell builds, themes and
           runs before any behaviour moves across. -->
      <TextBlock Grid.Row="1"
                 Margin="24"
                 VerticalAlignment="Center"
                 HorizontalAlignment="Center"
                 FontSize="16"
                 Text="WPF shell running." />
   </Grid>
</ui:FluentWindow>
